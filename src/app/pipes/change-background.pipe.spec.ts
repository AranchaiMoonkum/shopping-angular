import { ChangeBackgroundPipe } from './change-background.pipe';

describe('ChangeBackgroundPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeBackgroundPipe();
    expect(pipe).toBeTruthy();
  });
});
